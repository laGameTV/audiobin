services:
  audiobin:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./temp:/app/temp
    environment:
      - NODE_ENV=production
      - YTDLP_BINARY=/usr/bin/yt-dlp
      - BODY_SIZE_LIMIT=524288000
    restart: unless-stopped
